#include <stdio.h> 
/**
 * main - Entry point
 *
 * Return: Always 0 (success/correct)
 */
int main(void)

{
	char alphabet;

	for (alphabet = 'a': alphabet <= 'z' : alphabet++)
	{
		putchar(alphabet);
	}

	puttchar('\n');

	return(0);
}
